<!-- What changes, and why. Write for the reviewer who was not in the room. -->

## What changes

## Deploy notes

<!-- Migrations (each one is listed in docker-entrypoint.sh), environment
     variables, anything to run or check after the restart. "None" is a
     fine answer. -->

## Checks

<!-- All of these should be green. If one is red, say why in the PR rather
     than merging over it:
     - Typecheck · Unit Tests · Build
     - Integration Tests
     - ESLint (fails on errors; warnings are reported, not failed)
     - TypeScript
     - Dependency CVE Scan, Container Image Scan, Static Analysis
     - PR Size & Commit Check (a warning above 800 changed lines) -->
